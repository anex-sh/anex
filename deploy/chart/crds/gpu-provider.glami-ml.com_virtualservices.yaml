---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: virtualservices.gpu-provider.glami-ml.com
spec:
  group: gpu-provider.glami-ml.com
  names:
    kind: VirtualService
    listKind: VirtualServiceList
    plural: virtualservices
    shortNames:
    - vsvc
    singular: virtualservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VirtualService is the Schema for the virtualservices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualServiceSpec defines the desired state of VirtualService
            properties:
              gateway:
                description: Gateway identifies which gateway pod(s) should handle
                  this VirtualService
                properties:
                  selector:
                    additionalProperties:
                      type: string
                    default:
                      gpu-provider-gateway: "true"
                    description: |-
                      Selector is a label selector for identifying the gateway pod(s)
                      In practice, should select exactly one gateway pod
                    type: object
                required:
                - selector
                type: object
              service:
                description: Service defines the Service-like specification for virtual
                  pods
                properties:
                  ports:
                    description: Ports is the list of ports exposed by this service
                    items:
                      description: ServicePort defines a port exposed by the VirtualService
                      properties:
                        name:
                          description: Name is an optional name for this port
                          type: string
                        port:
                          description: Port is the service port number
                          maximum: 65535
                          minimum: 1
                          type: integer
                        protocol:
                          default: TCP
                          description: |-
                            Protocol is the IP protocol for this port. Must be TCP.
                            Defaults to TCP.
                          enum:
                          - TCP
                          - ""
                          type: string
                        targetPort:
                          description: TargetPort is the port number on the virtual
                            pod (must be an int)
                          maximum: 65535
                          minimum: 1
                          type: integer
                      required:
                      - port
                      - targetPort
                      type: object
                    minItems: 1
                    type: array
                  selector:
                    additionalProperties:
                      type: string
                    description: Selector is a label selector for virtual pods
                    type: object
                required:
                - ports
                - selector
                type: object
            required:
            - gateway
            - service
            type: object
          status:
            description: VirtualServiceStatus defines the observed state of VirtualService
            properties:
              allocatedPorts:
                description: AllocatedPorts maps each service port to its allocated
                  gateway port
                items:
                  description: AllocatedPort represents a mapping between service
                    port and allocated gateway port
                  properties:
                    gatewayPort:
                      description: GatewayPort is the allocated port on the gateway
                      type: integer
                    name:
                      description: Name is the optional name of the port
                      type: string
                    protocol:
                      description: Protocol is the IP protocol
                      type: string
                    servicePort:
                      description: ServicePort is the port number from the service
                        spec
                      type: integer
                    targetPort:
                      description: TargetPort is the target port on the virtual pod
                      type: integer
                  required:
                  - gatewayPort
                  - servicePort
                  - targetPort
                  type: object
                type: array
              conditions:
                description: Conditions represents the latest available observations
                  of the VirtualService's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed VirtualService
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
