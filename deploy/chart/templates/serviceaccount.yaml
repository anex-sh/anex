apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ include "chart.fullname" . }}"
  namespace: {{ .Values.namespace }}
  annotations: {{ toYaml .Values.virtualKubelet.serviceAccount.annotations | nindent 4 }}
